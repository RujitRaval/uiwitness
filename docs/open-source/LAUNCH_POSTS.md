# Launch Posts

These are the approved public launch copies for the current protected customer release, `v0.26.13`. Preserve the authorization and evidence-privacy boundary at the end of every post. Edit product detail for a channel only if both boundary sentences remain intact.

## Repository and release announcement

UIWitness `v0.26.13` finds, renders, and reports the UI states a product forgot. Point Quick Check at an authorized public site for a local mobile/desktop × light/dark evidence report, or define loading, empty, error, authenticated, and long-content scenarios for durable product-state coverage. The CLI, Contract Guard, report, and GitHub Action stay deterministic and local-first—no account, hosted backend, telemetry, or required LLM.

Install the protected release with `npm install --save-dev --save-exact uiwitness@0.26.13 playwright@1.62.1`, then run `npx playwright install chromium`.

Test only software you own or are authorized to assess. UIWitness keeps evidence local by default, but screenshots, URLs, and diagnostics may contain sensitive data; review them before sharing or uploading.

## Professional network announcement

Most UI checks prove the happy path twice. UIWitness asks a different question: did every important product state survive the viewport, theme, and data conditions the team actually supports?

The open-source `v0.26.13` release produces one offline evidence report for configured loading, empty, error, unauthorized, long-content, responsive, and theme states. It also includes a bounded Quick Check for authorized public sites and a contract guard that makes UI promises explicit in CI. Everything runs locally without an account, cloud service, telemetry, or required LLM.

Test only software you own or are authorized to assess. UIWitness keeps evidence local by default, but screenshots, URLs, and diagnostics may contain sensitive data; review them before sharing or uploading.

## Community launch announcement

UIWitness is an open-source, local-first product-state coverage tool. `v0.26.13` can check an authorized public surface in one command, promote the useful routes into config, exercise explicit application states with Playwright, and produce a self-contained evidence report. Four deliberate failures in the fictional Northline example show the narrow mobile and theme-specific defects a green happy path misses.

Start with `npx uiwitness check <url>` after installing the exact protected release, or use the configured scan and Contract Guard workflows for deeper coverage.

Test only software you own or are authorized to assess. UIWitness keeps evidence local by default, but screenshots, URLs, and diagnostics may contain sensitive data; review them before sharing or uploading.
